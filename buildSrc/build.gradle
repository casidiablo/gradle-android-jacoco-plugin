repositories {
    maven {
        url "/home/acsia/dev/android/source/out/host/gradle/repo/"
    }
}

rootProject.dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:0.9.3-SNAPSHOT'
    runtime project(':jacoco')
}